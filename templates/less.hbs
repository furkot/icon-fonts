/*
 * {{fontName}} icon font glyphs
 * Do not edit. Automatically generated by furkot-icon-fonts@{{version}}
 * See: https://github.com/furkot/icon-fonts
 */

@font-face {
  font-family: "{{fontName}}";
  src:
    url("{{fontsPath}}{{fontName}}.woff2") format("woff2"),
    url("{{fontsPath}}{{fontName}}.woff") format("woff");
  font-weight: normal;
  font-style: normal;
}

@icon-font-styles: {
  font-style: normal;
  font-weight: normal !important;
  font-variant: normal;
  line-height: 1;
  text-transform: none;
  speak: none;
  -webkit-font-smoothing: antialiased;
};

{{#each codepoints}}
.{{../classPrefix}}{{@key}},
{{/each}}
.{{baseSelector}} {
  font-family: {{fontName}} !important;
  @icon-font-styles();
}

{{#each codepoints}}
.{{../classPrefix}}{{@key}}:before {
  content: "\\{{this}}";
}
{{/each}}

{{#if alias}}
.alias-{{baseSelector}}(@char) {
  &:before {
    @icon-font-styles();
    font-family: {{fontName}} !important;
    content: @char;
  }
}

{{#each alias}}
{{@key}} {
  .alias-{{../baseSelector}}("\\{{lookup ../codepoints this}}");
}
{{/each}}
{{/if}}
